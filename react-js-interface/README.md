# React JS Interface (Simplified)

This is a plain JavaScript React rebuild of the interface in a separate folder.

## Run

1. `cd react-js-interface`
2. `npm install`
3. `npm run dev`

## Build

- `npm run build`
